import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './src/ej1/App'; //Change the folder and the n° of the App file to see each exercise 

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

